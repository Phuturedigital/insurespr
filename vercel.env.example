# Server-only Vercel Function variables for the same-origin verification bridge.
# Configure these in Vercel for Production and Preview; never expose them as
# browser variables, commit real values, or copy them into issue comments.
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
INSURESPR_PROXY_PRIVATE_KEY_B64=

